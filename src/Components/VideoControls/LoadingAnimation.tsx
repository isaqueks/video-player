import style from '@Styles/VideoControls/LoadingAnimation.module.css'

interface Props {
}

export default function LoadingAnimation(props: Props) {
    return (
        <div className={style.loadingAnimation} />
    );
}